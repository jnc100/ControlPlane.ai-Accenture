pyyaml>=6.0
rich>=13.0
requests>=2.31.0

# Optional — only needed if you set ANTHROPIC_API_KEY (paid) instead of
# GROQ_API_KEY (free) for the real LLM-judge path. Not required to run the
# demo; the simulated judge mode works with zero extra installs.
# anthropic>=0.40.0
